Đặt ảnh cưới vào đây, đặt tên album-01.jpg, album-02.jpg... (hoặc sửa lại đường dẫn trong js/config.js -> gallery.images). Nên nén ảnh trước khi tải lên để trang tải nhanh.

THÊM: og-cover.jpg — ảnh hiển thị khi chia sẻ link thiệp cưới qua Zalo/Facebook/Messenger
(thẻ og:image trong <head> của index.html, rsvp.html, guestbook.html). Kích thước khuyến
nghị 1200×630px, dung lượng dưới ~300KB. Nếu không thêm ảnh này, một số app sẽ không hiện
ảnh preview khi khách dán link.
